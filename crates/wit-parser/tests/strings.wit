package foo:foo;

/// regular comment
/// another comment
///#[derive(Serialize, Deserialize)]
///#[path="ui"]
interface strings {
  a: func(x: string);
  b: func() -> string;
  c: func(a: string, b: string) -> string;
}

world the-world {
  import strings;
  export strings;
}
